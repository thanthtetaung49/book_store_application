class MainBook {
  final String title, imagePath, category, description, pdfFilePath, author;
  final int rating;

  MainBook(this.title, this.imagePath, this.category, this.description, this.pdfFilePath, this.author, this.rating);
}